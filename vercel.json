{
  "buildCommand": "mkdir -p dist/public",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/", "destination": "/api/maintenance" },
    { "source": "/:path*", "destination": "/api/maintenance" }
  ]
}
